# task_links(SQLite永続化) Step 1 (DB層の追加)
。
## 対象
- `kadai_app/src/lib/db.ts`

## やること
- `task_links` テーブル作成
- `listTaskLinks()` 実装

## 成功条件
- `npm run build --prefix kadai_app` が通る
- `listTaskLinks()` が実行できる（型が通る）

